# Arquivo

Contexto antigo, fora do startup normal do agente. Sessões já compactadas para `.ai/history/` são movidas para cá em vez de deletadas.

Consulte apenas em investigação específica. **Nunca carregue este diretório por padrão.**

Antes de mover algo para cá, confirme que nenhuma task aberta (`TODO`, `READY`, `PARTIAL`, `BLOCKED`) depende do conteúdo.

Vazio por enquanto.
